export class Campaign {
  constructor(props) {
    this.characterName = props.characterName;
    this.difficulty = props.difficulty;
    this.characterClass = props.characterClass;
    this.startTime = props.startTime;
    this.balance = props.balance;
    this.inventory = props.inventory;
  }
}

export const Difficulty0 = new Campaign({

});
export const Difficulty1 = new Campaign({

});
export const Difficulty2 = new Campaign({

});
export const Difficulty3 = new Campaign({

});
export const Difficulty4 = new Campaign({

});
